{% extends 'base.html.twig' %}

{% block title %}County index{% endblock %}

{% block body %}
    {{ include('misc/menu.html.twig') }}
    <h1>County index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for county in counties %}
            <tr>
                <td>{{ county.id }}</td>
                <td>{{ county.name }}</td>
                <td>
                    <a href="{{ path('app_county_crud_show', {'id': county.id}) }}">show</a>&nbsp;|&nbsp;
                    <a href="{{ path('app_county_crud_edit', {'id': county.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_county_crud_new') }}">Create new</a>
{% endblock %}
