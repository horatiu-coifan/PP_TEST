{% extends 'base.html.twig' %}

{% block title %}County{% endblock %}

{% block body %}
    {{ include('misc/menu.html.twig') }}
    <h1>County</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ county.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ county.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_county_crud_index') }}">back to list</a>

    <a href="{{ path('app_county_crud_edit', {'id': county.id}) }}">edit</a>

    {{ include('county_crud/_delete_form.html.twig') }}
{% endblock %}
