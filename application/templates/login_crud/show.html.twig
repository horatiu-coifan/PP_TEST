{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    {{ include('misc/menu.html.twig') }}
    <h1>Login</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ login.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ login.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ login.password }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ login.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_login_crud_index') }}">back to list</a>

    <a href="{{ path('app_login_crud_edit', {'id': login.id}) }}">edit</a>

    {{ include('login_crud/_delete_form.html.twig') }}
{% endblock %}
