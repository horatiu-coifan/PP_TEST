{% extends 'base.html.twig' %}

{% block title %}Products{% endblock %}

{% block body %}
    {{ include('misc/menu.html.twig') }}
    <h1>Products</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_products_crud_index') }}">back to list</a>

    <a href="{{ path('app_products_crud_edit', {'id': product.id}) }}">edit</a>

    {{ include('products_crud/_delete_form.html.twig') }}
{% endblock %}
